# 2026-07-20 — HT2 Rebuild Continuation

## Completed
- **Cell flip fix:** negated Z + 180° offset → cells correctly positioned on Earth texture
- **Deeper zoom:** added Cell (6420km), Close (6500km), Local (6700km) zoom rungs
- **Tier 1-2 optimization:** batched ArrayMesh build, mesh pool, chunk-based cache (64×256)
- **Rebuild-on-stop:** deferred mesh generation until camera settles for 0.3s
- **Pentagon halving:** `mini(segs_bot, segs_top)` → clean pentagons at halving bands
- **Tier 3 LOD:** Python atlas generator + Godot lod_display.gd with pre-baked terrain textures
- **Egypt fix:** Natural Earth Egypt geometry was `is_valid=False`, was being filtered out. Now `buffer(0)` repairs.
- **Chunk visibility:** replaced broken band/seg math with arc-distance check
- **Multi-chunk display:** replaced 2-node pool with per-chunk MeshInstance3D nodes
- **LOD mesh fixes:** shared vertex rings, pole fan triangulation, mipmap filtering
- **Aggressive preload:** neighbors pre-fetch immediately after visible area cached
- **Increased margins:** 300km chunk margin, 1500km visible radius cap

## Visual Display Modes — Architecture
- **Mode system:** `_display_mode` int on earth_display.gd, `_input()` captures KEY_0–KEY_9, `set_display_mode()` invalidates chunk cache on switch
- **terrain_loader** wired in (mode 1)
- **climate_loader** wired in — 30 Köppen zones (mode 2)
- **weather_loader** wired in — 595 MB, 4 vars × 12 months (modes 3-6)
- **Mode name overlay:** CanvasLayer Label showing single-word names (Land/Elevation/Climate/Temperature/Precipitation/Wind/Daylight/Slope), centered, bold black, fades out over 2.5s

## Visual Display Modes — Modes 0-7
- **0: Land/Sea** — simple green land / blue ocean
- **1: Elevation** — 11-level terrain palette (deep blue → white peaks)
- **2: Climate** — 30 Köppen zones, distinct colour per zone
- **3: Temperature** — heatmap: blue(-20°C)→green(10°C)→red(40°C), annual avg
- **4: Precipitation** — blue ramp: pale blue(0mm)→deep blue(3000mm), annual total
- **5: Wind** — grey(0)→magenta(3 m/s), annual avg (real range 0-2.3 m/s)
- **6: Daylight** — dark(0)→orange→yellow(35000 kJ/m²/d), annual avg solar radiation (WorldClim int16 values, real range 0-32767)
- **7: Slope** — beige-yellow(flat)→pale purple(cliff), 5 levels (NB: all flat — terrain generated in fast mode)

## Bug Fixes
- **Halving-band chunk visibility:** dual-center check for rows straddling halving rings — pre-halving and post-halving centers both tested
- **lod_display.gd string format error:** `"%s" % lod_list` → `"%s" % str(lod_list)` for Array
- **Input capture:** `_unhandled_input` → `_input` (MCP Control tree consumed unhandled events)
- **Wind ramp:** 0→20 m/s → 0→3 m/s (matched real data range)
- **Solar ramp:** 0→30 → 0→35000 kJ/m²/d (matched real WorldClim int16 values)

## Next Session
- Chunk loading/unloading threshold tweaks — ensure all visible chunks loaded, no premature unloading
- Mode-switching performance — cells are fixed per mode, could be pre-baked as textures
- LOD system is broken — needs investigation and fix (likely source of lag spikes)
- Time system — port from Stella Nostra / previous HT implementation
- Day/Night cycle — after time system is in place
